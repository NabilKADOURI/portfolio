{% block main %}
<section class="experience" id="experience">
	<div class="container">
		<h2 class="section-title mb-5">Mes
			<span>expériences</span>
		</h2>
		<div class="row text-center experience-content my-5">
			{% for experience in experiences %}
				<div class="col-lg-3 ">
					<div class=" mb-4 experience-box ">
					<a target="_blank" href="{{path('app_experience', {id:experience.id})}}" ><i class="{{experience.icon}}" ></i></a>
						
						<h3>{{experience.name}}</h3>
						<h4>{{experience.startYear|date("Y")}}-{{experience.endYear|date("Y")}}</h4>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
</section>
{% endblock %}
